import 'package:flutter/material.dart';
class StaffRegistrationScreen extends StatelessWidget {
  const StaffRegistrationScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Staff Registration - Stub')));
}
