class SheetsService {
  Future<List<dynamic>> getProgrammes() async {
    await Future.delayed(
      const Duration(milliseconds: 500),
    );

    // TODO: Replace with data from integration team
    return [];
  }

  Future<List<dynamic>> getOpportunities() async {
    await Future.delayed(
      const Duration(milliseconds: 500),
    );

    // TODO: Replace with data from integration team
    return [];
  }
}