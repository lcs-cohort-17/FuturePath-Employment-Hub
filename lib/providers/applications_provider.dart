import 'package:flutter/material.dart';

class ApplicationsProvider extends ChangeNotifier {
  Future<void> addApplication(
      String userId,
      String jobId,
      String jobTitle,
      String status,
      ) async {
    // Backend/data team will connect this later.
  }
}